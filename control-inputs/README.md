# Synthetic Control inputs

This directory contains public, sanitized inputs used to bind one S4 synthetic execution attempt to its completion contract and expected evidence.

These files are not Human decisions, Control receipts, or protected record entries. They carry no authority by themselves. The protected record repository remains separate, and only the restricted Recorder path may publish validated formal records.

The current pair authorizes evidence accounting for the bounded G3 static prototype that follows the exact Human-approved G2 source commit. It excludes backend implementation, persistence, deployment, real data, the future 70% threshold proposal, later Gates, and Control adoption.
