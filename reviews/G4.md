# G4 fixed review summary — proposed

Project: `S4-P-001`  
Submission: `SUB-S4-G4-001`  
Gate: `G4`  
Planned task after approval: `BUILD-SLICE@R1 / IMPLEMENT_SLICE`  
Decision status: `PENDING_HUMAN_REVIEW`

The prior G3 decision is `批准 SUB-S4-G3-001 @51f32cafc7747d639989635ef85f99140d75b62c`. It authorized preparation of the technical plan and frozen completion contract only. No G4 implementation has started.

## Route proposed for approval

If Human later approves the exact G4 package, the Builder may implement one public-synthetic first slice on one Mac:

1. a Python standard-library HTTP service bound only to `127.0.0.1`;
2. real raw-CSV import through HTTP;
3. atomic rejection of the approved negative and duplicate-key batches;
4. exact Decimal monthly totals, comparisons, and the current strict `>30.000%` rule;
5. an immutable SQLite record written in one transaction;
6. real API and browser readback;
7. application-process exit and restart against the same database, followed by the same-ID result reload.

The slice installs no dependency, calls no external service, changes no Tunnel or Control topology, and makes no authentication claim. The actor header is a declarative workflow check and remains labelled `IDENTITY_ENFORCEMENT_NOT_PROVEN`.

## Fixed boundaries

- The exact plan is `specs/implementation.md`: 17,699 bytes, SHA-256 `169934a9bd1ca366c261c8178a5cea4c804d01891c5f0c6d2cd81b5a97e0a141`.
- The frozen completion contract is `control-inputs/G4-FIRST-SLICE-completion-contract.json`: 1,435 bytes, SHA-256 `1207e54f83f63c23df5ec8ba176ca978a0806f193a1a5d45ad31830153c946f2`.
- The first slice proves only the named valid, negative, duplicate, real-browser, SQLite, and restart cases. Remaining G2 cases stay in later batches and still require G5 approval.
- The 65,536-byte HTTP ceiling is a slice resource boundary, not a new G2 business limit.
- SQLite stores arbitrary-precision integer-thousandths as canonical text and never calculates with binary float.
- G2 did not bound the number of input digits. The first slice does not claim the full huge-number domain; a later full claim needs independent proof or a separate G2 clarification.
- Reviewer distinctness and the single-observation preparation check remain `AGENT_COMPLIANCE` process controls because the current Control schema does not make that separation technically unbypassable.

## Review result

The first independent Sol high review found four P2 traceability defects: incomplete machine-contract coverage, overlap between first and later slices, a SQLite integer-range mismatch, and a stronger reviewer-separation claim than Control enforces. The plan and frozen contract were corrected. A fresh independent Sol high review returned `PASS` with zero P0, P1, P2, or P3 findings. The final review also confirmed that the plan exposes the numeric-domain and process-control limits rather than hiding them.

## Effect of a later G4 approval

A valid G4 decision would authorize only `BUILD-SLICE@R1 / IMPLEMENT_SLICE` for the first slice described above and use the frozen completion contract for its evidence. It would not approve the resulting implementation, release later batches, pass G5 or G6, deploy the sample, use real business data, adopt Factory Control, or approve the separate 30-to-70-percent G2 change.

The Owner issue, frozen package commit, issue binding, and copyable decision line are added through the authorized records path after this draft is frozen. This page does not create or imply a Human decision.
