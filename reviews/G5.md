# G5 first-slice joint review package — proposed

Project: `S4-P-001`

Submission: `SUB-S4-G5-001`

Gate: `G5`

Current task: `BUILD-SLICE@R1 / IMPLEMENT_SLICE`

Task proposed after approval: `BUILD-EXPAND@R1 / IMPLEMENT_EXPAND`

Decision status: `PENDING_HUMAN_REVIEW`

The exact Human G4 decision approved package `2b7ad3eec9b25a79c2a458de5aebd318366a9ea4` and authorized only the first slice. The implementation result proposed for G5 is source commit `0f4547851c267c2094d1716ff0f1e14fc2e40bcc`, tree `e0c8675bb10dd9016377d9491a7252ed9d491a0d`, and the single successful execution observation `OBS-S4-G4-FIRST-SLICE-004`.

## Result proposed for acceptance

- Real loopback HTTP imported the approved public synthetic CSV.
- The negative batch returned `422 / NEGATIVE_KWH`; fresh history and byte-for-byte SQLite checks proved no mutation.
- The accepted dataset persisted exact totals `300.000` and `370.000 kWh`, A growth `60.000%` anomalous, and B growth `5.000%` normal.
- A second application process re-opened the same database and returned the same immutable ID, bytes, API result, canonical calculation, and browser result.
- The real Chrome path covered empty state, rejection, import, history, keyboard focus, narrow layout, table scrolling, and restart readback.
- Independent technical and product reviews both returned PASS with zero open P0-P3 findings.
- The exact online Control `CONSUME` for this first slice is `ALLOW / SATISFIED` on the corrected S4 acceptance build, with the formal readback and limits in [G5 online Control addendum](G5-online-control.md). This is a technical completion result, not the Human G5 decision.

The public package contains only synthetic source, tests, sanitized summaries, selected screenshots, and formal-record inputs. The complete 67-file operational evidence remains private; its sealed manifest SHA-256 is `805fb25735727046f7c5e9975623e56dc59a3a17a3c5dc2f2bb247f6f15eb89c`.

## Preserved negative history

OBS-002 remains a genuine failed earlier candidate and OBS-003 remains an insufficient evidence attempt. Neither was renamed as an intentional defect, deleted, or used as completion evidence. The protected preparation check rejects a failed or wrong source candidate, multiple observations, a mismatched observation, and collapsed technical/product verification identities; only the exact OBS-004 package is eligible.

## Limits retained

- The app is a public-synthetic loopback sample with no login; `IDENTITY_ENFORCEMENT_NOT_PROVEN` remains visible.
- The first slice does not cover the full G2 catalog, remote deployment, real business data, multi-writer operation, or the full unbounded numeric domain.
- The table focus and arrow-key behavior are proven; the full Tab traversal into the table was not separately captured, so this is not a full accessibility certification.
- The source candidate, review package commit, Human Gate decision, and formal Control records are separate identities.
- A Control readiness result or either independent PASS cannot substitute for the Human G5 decision.

## Effect of a later exact G5 approval

A valid Human approval of this exact submission would accept the bounded first-slice result and authorize `BUILD-EXPAND@R1` only for the remaining work already listed in the approved G4 route: remaining error-priority cases, replay and conflict variants, quoted fields, exact-decimal and 30 percent threshold edges, missing-month and zero-baseline cases, the complete Analyst/Reviewer action matrix, browser accessibility refinements, packaging, and full regression.

It would not approve G6, the separate 30-to-70 percent G2 change, any deployment, authentication claim, real business use, production binding, or Factory Control adoption.
