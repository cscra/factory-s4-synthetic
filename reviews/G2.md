# G2 fixed review summary — proposed

Project: `S4-P-001`
Submission: `SUB-S4-G2-001`
Gate: `G2`
Material: detailed synthetic product requirements and acceptance baseline
Decision status: `PENDING_HUMAN_REVIEW`

The prior G1 provenance supplied for this preparation is `批准 SUB-S4-G1-001 @d22e8c591309b5a555d04474e5e172159b008afc`. It authorizes preparation of G2 material only. This page does not create or imply a G2 Human decision.

## Proposed G2 scope

Approve, if Human later chooses to do so, the exact contract in `specs/requirements.md` and the scenarios in `specs/acceptance.md`:

- exact `month,building,kwh` rows and strict `YYYY-MM` months;
- non-negative decimal kWh values with at most three fractional digits;
- Decimal or fixed-point arithmetic without binary floating-point;
- duplicate row-key rejection, same-dataset replay rejection, and changed-replay conflict rejection;
- whole-batch atomic failure;
- monthly totals and same-building immediate-calendar-month comparisons;
- explicit `NO_PRIOR_MONTH` and `ZERO_BASELINE` states with no fabricated 0% result;
- strict `>30.000%` anomaly criterion, with the primary fixture flagging only Building A.

The fixtures are synthetic public inputs and independent expected values. They are not implementation evidence.

## Separate future gate

The proposed change from 30% to 70% is tracked separately in `tasks/G2-THRESHOLD-CHANGE-70.json`. It is a future Human gate, has no current decision, and is excluded from the present G2 scope. A current G2 acceptance must not be written as approving 70%.

## Binding status

The Owner has not created the G2 records page in this draft. All G2 binding fields remain pending: `issue_id=null`, `url=null`, `decision_repository_id=null`, and `source_commit=null`. The page and accurate decision line must be added through the authorized records path later; this public repository must not manufacture them.
